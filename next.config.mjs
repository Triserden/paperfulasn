/** @type {import('next').NextConfig} */
const nextConfig = {
      output: "export",
      basePath: "/paperfulasn",
      images: {
            unoptimized: true,
      },
};

export default nextConfig;
